import React from 'react';
import { StyledSection } from '../styledComponent/Section';

const Work = function Work() {
  return <StyledSection id="work">work</StyledSection>;
};

export default Work;
