import React from 'react';
import { StyledSection } from '../styledComponent/Section';

const Home = function Home() {
  return <StyledSection id="home">home</StyledSection>;
};

export default Home;
